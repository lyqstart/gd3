# Firebase移除改进计划

## 背景

**日期**: 2026-01-14  
**问题**: 构建Web版本时遇到Firebase编译错误,且代码违反需求9.9

### 需求文档要求

**需求9.9**: "THE System SHALL 不依赖第三方公有云服务(如Firebase、AWS等),所有数据存储在企业自建服务器"

### 当前问题

1. **构建失败**: Firebase包与Flutter Web编译器不兼容
2. **需求违反**: 代码使用Firebase,违反需求9.9
3. **已有替代方案**: C# ASP.NET Core后端 + MySQL数据库

---

## 移除计划

### 第1步: 移除Firebase依赖包

**文件**: `pubspec.yaml`

**移除以下依赖**:
```yaml
firebase_core: ^2.24.2
firebase_auth: ^4.15.3
cloud_firestore: ^4.13.6
```

### 第2步: 删除Firebase服务文件

**删除以下文件**:
1. `lib/services/firebase_auth_service.dart` - Firebase认证服务
2. `lib/services/firestore_sync_service.dart` - Firestore同步服务
3. `lib/firebase_options.dart` - Firebase配置文件

### 第3步: 修改认证状态管理器

**文件**: `lib/services/auth_state_manager.dart`

**修改内容**:
- 移除FirebaseAuthService依赖
- 使用C# API进行认证(通过HTTP请求)
- 保持相同的接口,确保UI层无需修改

### 第4步: 修改云同步管理器

**文件**: `lib/services/cloud_sync_manager.dart`

**修改内容**:
- 移除FirestoreSyncService依赖
- 仅使用MySQL同步(通过C# API)
- 保持相同的接口,确保调用方无需修改

### 第5步: 清理和重新构建

**操作**:
1. `flutter clean` - 清理构建缓存
2. `flutter pub get` - 重新获取依赖
3. `flutter build web --release` - 构建Web版本
4. `flutter build apk --release` - 构建Android APK

---

## 预期结果

### ✅ 符合需求

- 完全符合需求9.9
- 不依赖第三方公有云服务
- 所有数据存储在企业自建服务器

### ✅ 功能完整

- 认证功能通过C# API实现
- 数据同步通过MySQL实现
- 所有核心功能保持不变

### ✅ 构建成功

- Web版本可以正常构建
- Android APK可以正常构建
- 无Firebase编译错误

---

## 风险评估

### 🟢 低风险

**理由**:
1. C# API已实现认证功能
2. MySQL同步已实现并测试通过
3. 仅需修改服务层,UI层无需改动
4. 接口保持不变,影响范围可控

### 预计工作量

- 修改代码: 1-2小时
- 测试验证: 1-2小时
- 总计: 2-4小时

---

**计划制定人**: Kiro AI Assistant  
**计划日期**: 2026-01-14  
**计划版本**: 1.0
