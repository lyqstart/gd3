# 油气管道开孔封堵计算系统 - 环境变量配置模板
# 请复制此文件为 .env 并填入实际的配置值

# ============================================
# 数据库配置
# ============================================

# MySQL 数据库主机地址
DB_HOST=localhost

# MySQL 数据库端口
DB_PORT=3306

# MySQL 数据库名称
DB_NAME=pipeline_calc

# MySQL 应用用户名（用于应用程序连接）
DB_USERNAME=pipeline_app_user

# MySQL 应用用户密码（请使用强密码）
DB_PASSWORD=314697

# MySQL Root 用户密码（仅用于初始化数据库，生产环境不应使用）
DB_ROOT_PASSWORD=314697

# ============================================
# 后端 API 配置
# ============================================

# JWT 密钥（用于生成和验证 Token，请使用至少32字符的随机强密钥）
# 生成方法: openssl rand -base64 32
JWT_SECRET_KEY=your_jwt_secret_key_minimum_32_characters_long

# JWT 发行者
JWT_ISSUER=PipelineCalculationAPI

# JWT 受众
JWT_AUDIENCE=PipelineCalculationApp

# JWT Token 过期时间（单位：分钟）
JWT_EXPIRY_MINUTES=60

# API 服务器监听地址和端口
ASPNETCORE_URLS=http://0.0.0.0:5000

# 运行环境（Development, Staging, Production）
ASPNETCORE_ENVIRONMENT=Production

# ============================================
# 连接池配置
# ============================================

# 最小连接数
DB_MIN_POOL_SIZE=5

# 最大连接数
DB_MAX_POOL_SIZE=20

# 连接超时时间（单位：秒）
DB_CONNECTION_TIMEOUT=30

# ============================================
# SSL/TLS 配置（可选）
# ============================================

# 是否启用 SSL 连接
DB_USE_SSL=false

# SSL 证书路径（如果启用 SSL）
# DB_SSL_CA_PATH=/path/to/ca-cert.pem
# DB_SSL_CERT_PATH=/path/to/client-cert.pem
# DB_SSL_KEY_PATH=/path/to/client-key.pem

# ============================================
# 日志配置
# ============================================

# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# 是否启用数据库查询日志
DB_QUERY_LOG=false

# ============================================
# 安全提示
# ============================================
# 1. 不要将 .env 文件提交到版本控制系统
# 2. 使用强密码，至少包含大小写字母、数字和特殊字符
# 3. 定期更换密码和密钥
# 4. 生产环境不要使用默认值
# 5. 限制数据库用户权限，仅授予必要的操作权限
